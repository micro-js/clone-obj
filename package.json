{
  "name": "@micro-js/clone-obj",
  "description": "Shallow clone an object",
  "repository": "git://github.com/micro-js/clone-obj.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@micro-js/foreach": "0.0.1"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
